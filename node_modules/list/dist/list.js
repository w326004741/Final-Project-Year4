"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
var Cons = /** @class */ (function () {
    function Cons(value, next) {
        this.value = value;
        this.next = next;
    }
    Cons.prototype.toArray = function () {
        var array = [];
        var cur = this;
        while (cur !== undefined) {
            array.push(cur.value);
            cur = cur.next;
        }
        return array;
    };
    Cons.prototype.nth = function (index) {
        var cur = this;
        for (var i = 0; i < index && cur !== undefined; ++i) {
            cur = cur.next;
        }
        return cur === undefined ? undefined : cur.value;
    };
    return Cons;
}());
exports.Cons = Cons;
function copyFirst(n, list) {
    var newHead = new Cons(list.value, undefined);
    var current = list;
    var newCurrent = newHead;
    while (--n > 0) {
        current = current.next;
        var cons = new Cons(current.value, undefined);
        newCurrent.next = cons;
        newCurrent = cons;
    }
    return newHead;
}
exports.copyFirst = copyFirst;
function concat(a, b) {
    var list = new Cons(a.value, undefined);
    var prev = list;
    var cur = a;
    while ((cur = cur.next) !== undefined) {
        prev.next = new Cons(cur.value, undefined);
        prev = prev.next;
    }
    prev.next = b;
    return list;
}
exports.concat = concat;
//# sourceMappingURL=list.js.map